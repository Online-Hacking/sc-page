   // const allowedDomains = ['callspoofing.org', '127.0.0.1', '192.168.0.*'];
   // const currentDomain = window.location.hostname;
   // if (!allowedDomains.includes(currentDomain)) {
  //      window.location.href = 'https://callspoofing.org';
  //  }
